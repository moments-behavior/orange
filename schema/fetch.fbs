namespace FetchGame;

enum ServerControl : byte {
    IDEL = 0,
    START = 1,
    SETPTP = 2,
    QUIT = 3
}

table Server {
    control:ServerControl = IDEL;
    ptp_global_time:ulong;
}

root_type Server;