namespace FetchGame;

enum ServerControl : byte {
    IDEL = 0,
    START = 1,
    SETPTP = 2,
    STOP = 3,
    QUIT = 4
}

table Server {
    control:ServerControl = IDEL;
    ptp_global_time:ulong;
}

root_type Server;